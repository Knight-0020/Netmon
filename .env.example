# NetMon Configuration

# Database
POSTGRES_USER=netmon
POSTGRES_PASSWORD=netmon
POSTGRES_DB=netmon

# Agent Settings
# Update LAN_CIDR to match your home network (check with `ip a` or `ifconfig`)
LAN_CIDR=192.168.1.0/24
SCAN_INTERVAL_SEC=20
OFFLINE_AFTER_SEC=60

# Internet Health Checks
INTERNET_INTERVAL_SEC=5
INTERNET_DOWN_THRESHOLD=3
PING_TARGETS=1.1.1.1,8.8.8.8

# UI Settings (optional)
# API_BASE is read at runtime by the UI proxy (recommended for flexible deployments)
# NEXT_PUBLIC_API_BASE is read at build time if you want a fixed API base
API_BASE=
NEXT_PUBLIC_API_BASE=
